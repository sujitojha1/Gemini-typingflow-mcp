// shared_config.js — Default backend URL for the Chrome extension.
// Users can override this in Settings (saved to chrome.storage.sync).
// Loaded by: background.js (via importScripts) and options.html (via <script> tag).

const BACKEND_URL = 'http://localhost:8000';
